plugins {
    id 'com.android.application'
}

android {
    namespace 'com.familyframe.capability'
    compileSdk 35

    defaultConfig {
        applicationId 'com.familyframe.capability'
        minSdk 26
        targetSdk 35
        versionCode 5
        versionName '0.2.3-media-fix'
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}
